---
title: "神经网络期末复习：从BP到CNN的核心考点梳理"
published: 2026-05-27
description: "一门课的知识点全部浓缩进这篇文章：人工神经网络的定义与特点、M-P模型与激活函数、BP算法的原理与改进、CNN卷积网络的演进、自组织竞争网络、RBF径向基网络，以及遗传算法优化网络，附考试备考建议，考前冲刺看这一篇就够。"
tags: ["神经网络", "期末复习", "BP网络", "CNN", "深度学习"]
category: "学习"
draft: false
image: "/images/covers/wall-23.webp"
---

# 神经网络期末复习：从BP到CNN的核心考点梳理

期末周把我整个人按在图书馆椅子上疯狂摩擦，于是我决定把整门课的笔记彻底「榨干」——绪论、BP、CNN、自组织竞争网络、RBF 和遗传算法六大模块，加上老师给的模拟试题精华（含填空题原题、选择题原题、简答题评分标准和四类计算大题），全部浓缩成这一篇。考前翻完它，心里会踏实很多。放心，这篇笔记是删掉隐私之后才敢发出来的纯净版，只有知识点，没有个人信息。

## 一、绪论：神经网络到底是个啥

### ANN 的定义

**人工神经网络（ANN）**，听起来很高级，其实定义就一句话：它是**模仿人脑结构和功能的智能信息处理系统**，由大量简单的神经元互相连接而成。注意笔记里的原话还多一个定语——它是一个**非线性动力学系统**。考试填空最爱在这里挖空：「人工神经网络是对人脑的**一阶特性**描述，具有高度的**非线性**性、良好的**容错性**和**自学习**能力。」这三个词请刻进 DNA。

### 人脑 vs 计算机

这俩各有各的赛道：**人脑擅长形象思维**——联想、模糊、并行处理，看到一只猫就算它只露了半张脸也能认出来；**计算机擅长逻辑思维**——串行、精确，算 123456×789 眼睛都不眨。所以模拟题里那道选择题问「说法错误的是哪个」，答案是 C：**人脑擅长精确的数值计算、计算机擅长模糊识别**——正好说反啦，是人脑擅长模糊识别，计算机才擅长精确计算。另外两个常见选项也顺手记一下：人脑并行 vs 计算机串行、人脑分布式存储 vs 计算机集中式存储，这两条都是对的。

### 发展的四阶段

神经网络这门课也经历过人生的大起大落，四个阶段按年份记：

1. **启蒙期（1890–1969）**：想法萌芽，大家开始琢磨「机器能不能像大脑一样思考」。
2. **低潮期（1969–1982）**：Minsky 等人泼冷水，指出感知器的局限，研究进入寒冬，经费都没了。
3. **复兴期（1982–1986）**：Hopfield 网络和 BP 算法横空出世，寒冬结束，春天来了。
4. **高潮期（1987–）**：各种网络百花齐放，一直火到今天。

记法：一启蒙、二低谷、三复兴、四高潮，跟人生一样先甜后苦再翻盘。

### 基本特点与功能

**基本特点**（简答题常客，答出 3 点就给分，每点 1 分）：

- **分布存储与容错性**：知识存在一堆权值里，坏掉几个节点照样能用，像打不死的小强；
- **大规模并行处理**：所有神经元同时开工，不用排队；
- **自学习、自组织、自适应性**：给数据就能自己变强；
- **高度非线性全局作用**：能拟合各种弯弯绕绕的复杂关系。

**基本功能**：联想记忆（自联想/异联想）、非线性映射、分类与识别、优化计算、知识处理。五大金刚，一个都别落下（简答也是答出 3 点即可）。

## 二、基础知识：从生物神经元到 M-P 模型

### 生物神经元长啥样

生物神经元主要由四部分组成：**细胞体**（大脑中枢）、**树突**（输入通道，负责接收信号）、**轴突**（输出通道，负责把信号传出去）、**突触**（神经元之间的连接接口）。它工作起来就像一个小开关：膜电位慢慢积累，一旦**超过阈值（约 -55mV）**，就「啪」地一下产生兴奋脉冲；发完还得歇一歇，这就是**不应期**。

### M-P 模型：史上第一个神经元数学模型

M-P 模型把生物神经元抽象成六条假设，考试填空就在这挖坑：

1. 多输入单输出——一堆树突进来，一根轴突出去；
2. 兴奋/抑制性输入——有的信号是加油，有的是踩刹车；
3. 空间整合与阈值——所有输入先求和，够到阈值才触发；
4. 固定时滞——传导需要固定时间；
5. 忽略时间整合与不应期——为了简化，不管这些细节；
6. 非时变——参数不随时间变。

它的数学模型长这样：

$$O_j = f\left(\sum w_{ji}x_i - T_j\right)$$

其中 $w_{ji}$ 是连接权值（越权越重、说话越管用），$T_j$ 是阈值，$f$ 是激活函数。填空里那句「神经元的工作状态分为**静止**、**兴奋**和**抑制**三种；当膜电位超过 **-55**mV 时产生兴奋脉冲」，答案就是它（-55 写成「阈值」也算对）。

### 激活函数四兄弟

1. **阈值型（阶跃函数）**：要么 0 要么 1，干脆利落，但不可导——这是它最大的毛病；
2. **S 型**：最常用的主角！
   - 单极性 Sigmoid：$f(x)=\frac{1}{1+e^{-x}}$，导数有个神仙性质 $f'(x)=f(x)[1-f(x)]$，自己就能表示自己的导数，BP 算法全靠它；
   - 双极性 tanh：$\tanh(x)$，导数 $f'(x)=\frac{1-f(x)^2}{2}$，输出带正负号；
3. **线性型**：输出和输入成正比，简单；
4. **概率型**：用在玻尔兹曼机里，输出带随机性。

注意：因为 BP 要求激活函数**处处可微**，所以二值型阈值函数直接出局——这就是选择题里「不满足 BP 要求」的正确答案 D。干扰项还排着 Sigmoid、tanh 和线性函数，它们仨都满足要求。

### 网络拓扑与学习方式

**拓扑结构**分两大类：**层次型**（单纯层次型、输出到输入的反馈型、层内互联型）和**互连型**；从信号流向上又分**前向网络**和**反馈网络**。

**学习方式**有四种：

- **有导师学习**（监督）：老师给标准答案，错了就改；
- **无导师学习**（自组织）：没有答案，自己找规律聚类；
- **强化学习**：做对了给糖，做错了打手，靠奖惩摸索；
- **灌输式学习**：直接把知识设计进权值，典型代表是 Hopfield 网络的权值设计。

**学习规则**家族更热闹：Hebb 规则（「一起放电的神经元连在一起」）、感知器规则、**δ 学习规则**（BP 算法的核心，必考）、LMS 规则（Widrow-Hoff，最小均方）、胜者为王规则（竞争学习的基础）、内星/外星规则（CPN 用到）。每条规则都是一个小门派，掌门招式各不相同。

## 三、BP 网络：重点中的重点（考前必须背熟）

### 结构与两条铁律

BP 网络（反向传播网络）结构固定三件套：**输入层 → 隐层 → 输出层**，隐层可以一层也可以多层。工作流程就两句话：**前向传播算输出，反向传播调权值**——先算一遍看看差多少，再把误差从输出层一层层传回去改权值。

铁律一：激活函数必须**处处可微**，所以只能选 S 型（logsig / tansig），输出层也可以用线性函数；
铁律二：网络是全连接的，前向无反馈。

### 标准 BP 算法三件套（公式必背）

1. **误差函数**：$E = \frac{1}{2}\sum (d_k - o_k)^2$，期望输出 $d_k$ 和实际输出 $o_k$ 差的平方和再乘 1/2，就是为了求导方便；
2. **输出层误差信号**（对 logsig）：$\delta_k^o = (d_k - o_k)o_k(1-o_k)$——注意后面那坨 $o_k(1-o_k)$ 就是 Sigmoid 的导数，填空题就考它；
3. **隐层误差信号**：$\delta_j^y = y_j(1-y_j)\sum \delta_k^o w_{jk}$——误差从输出层**逐层反传**（填空答案：误差反向/反向传播），越传越淡；
4. **权值调整**：$\Delta w = \eta \delta \cdot \text{输入}$，学习率 $\eta$ 乘以误差信号再乘以本层输入，这就是 BP 的全部魔法。

### 批训练 vs 在线学习

**批训练**：攒够一批样本，用**总误差**统一调整一次权值，收敛更稳定，是考场常客；**在线学习**：来一个样本调一次，反应快但容易东摇西晃。考试问到就说：批训练更稳定。

### BP 的固有缺点（简答高频）

- 容易陷入**局部极小**——下山下到一个小坑就以为自己到谷底了；
- **收敛慢**——急死人；
- 存在**平坦区**——误差曲面有些地方几乎水平，怎么调都像踩在棉花上；
- 隐节点数**缺乏理论指导**——全靠试；
- 会**遗忘旧样本**——学了新的忘了旧的。

### 改进算法四件套

1. **附加动量法**：$\Delta w(t+1) = \eta\delta x + \alpha\Delta w(t)$，多了一个动量项 $\alpha\Delta w(t)$，像给下山的小球加了个惯性，能**冲过局部极小**（选择题选 B 那项）;
2. **自适应学习率**：误差降得快就加大 $\eta$，震荡了就减小 $\eta$，自动油门；
3. **陡度因子**：把净输入压缩，让神经元**退出饱和区**，别在 S 型函数的两端躺平；
4. **双极性 S 型压缩**：输出范围压到 ±1/2，收敛速度能**加快 30%~50%**，性价比极高。

**「平坦区」问题**值得单独展开（简答第 2 题原题）：它指的是误差曲面上某些区域**梯度变化很小**，就算权值调整量很大，误差下降依然缓慢，训练陷入停滞（这定义值 1 分）。产生原因是神经元输出进入了 **S 型变换函数的饱和区**（净输入绝对值太大），导致误差梯度 $\delta$ 接近 0，权值调整力度几乎为零（原因值 2 分）。克服方法就是上面四件套：附加动量、自适应学习率、陡度因子 $\lambda$ 压缩净输入，还可以改进误差函数（比如 Baum 误差函数），答出 3 点即可、每点 1 分。

### 网络设计经验（经验值，背就完了）

- **样本数**：$P = n_w/\varepsilon$（$n_w$ 是连接权总数），或者简单粗暴记：**连接权总数的 5~10 倍**；
- **隐层数**：最多 2 层，优先 1 层——能用一层解决的别整两层；
- **隐节点数**：试凑法！先设少一点，慢慢加，看网络误差最小定在哪。经验公式 $m = \sqrt{n+l} + \alpha$（$n$、$l$ 是输入/输出节点数，$\alpha$ 是 1~10 的常数）。选择题问最合理的做法，选「先少后多、按误差最小确定」，别选「越多越好」也别选「必须严格按公式」；
- **初始权值**：足够小且**正负相等**，让初始净输入落在零点附近，Sigmoid 才在最有活力的区间；
- **数据预处理**：尺度变换（归一化到 $[0,1]$ 或 $[-1,1]$）+ 分布变换（比如取对数）；
- **训练集/测试集划分**：通常 **2/3 训练、1/3 测试**，防止过拟合——考试别全拿来训练，得留点题测一测。

### 一道完整的 BP 计算题（必考题型，手把手带你算）

经典三层 BP：输入层 2 个节点 $x_1=0.3, x_2=0.7$，阈值节点 $x_0=-1$；隐层 2 个节点，阈值节点 $y_0=-1$；输出层 1 个节点，期望输出 $d=0.9$。激活函数都是单极性 Sigmoid $f(x)=\frac{1}{1+e^{-x}}$，学习率 $\eta=0.5$。

权值：$V=\begin{bmatrix} 0.1&0.4&-0.2\\ -0.3&0.5&0.2 \end{bmatrix}$（输入→隐层），$W=\begin{bmatrix} 0.2&0.6&-0.4 \end{bmatrix}$（隐→输出）。

**第一步，前向算隐层**：

$$\text{net}_{y1} = 0.1(-1)+0.4(0.3)+(-0.2)(0.7) = -0.12,\quad y_1 = f(-0.12) \approx 0.4700$$

$$\text{net}_{y2} = (-0.3)(-1)+0.5(0.3)+0.2(0.7) = 0.59,\quad y_2 = f(0.59) \approx 0.6434$$

**第二步，前向算输出层**：

$$\text{net}_o = 0.2(-1)+0.6(0.4700)+(-0.4)(0.6434) \approx -0.1753,\quad o = f(-0.1753) \approx 0.4563$$

$$E = \frac{1}{2}(0.9-0.4563)^2 \approx 0.0984$$

**第三步，反向算误差信号**：

$$\delta^o = (d-o)o(1-o) = (0.9-0.4563)\times0.4563\times0.5437 \approx 0.1101$$

$$\delta^{y1} = y_1(1-y_1)\delta^o w_{11} = 0.4700\times0.5300\times0.1101\times0.6 \approx 0.0165$$

$$\delta^{y2} = y_2(1-y_2)\delta^o w_{12} = 0.6434\times0.3566\times0.1101\times(-0.4) \approx -0.0101$$

**第四步，修正权值**（$\Delta = \eta \cdot \delta \cdot \text{输入}$）。注意输入是谁：输出层的输入是 $y_1=0.4700$、$y_2=0.6434$、阈值 $y_0=-1$；隐层的输入是 $x_1=0.3$、$x_2=0.7$、阈值 $x_0=-1$。全套 9 个修正量一次算给你看：

| 权值 | 修正量 | 权值 | 修正量 |
|---|---|---|---|
| $\Delta w_{10}$ | $0.5\times0.1101\times(-1) = -0.0551$ | $\Delta v_{10}$ | $0.5\times0.0165\times(-1) = -0.0083$ |
| $\Delta w_{11}$ | $0.5\times0.1101\times0.4700 = 0.0259$ | $\Delta v_{11}$ | $0.5\times0.0165\times0.3 = 0.0025$ |
| $\Delta w_{12}$ | $0.5\times0.1101\times0.6434 = 0.0354$ | $\Delta v_{12}$ | $0.5\times0.0165\times0.7 = 0.0058$ |
| | | $\Delta v_{20}$ | $0.5\times(-0.0101)\times(-1) = 0.0051$ |
| | | $\Delta v_{21}$ | $0.5\times(-0.0101)\times0.3 = -0.0015$ |
| | | $\Delta v_{22}$ | $0.5\times(-0.0101)\times0.7 = -0.0035$ |

套路就是：**误差信号 × 本层输入 × 学习率**，一层层来，稳如老狗。特别注意 $\Delta v$ 那两行的正负号：$\delta^{y2}$ 是负的，乘完就变号，别抄错。

### 模拟卷里的另一道 BP 题（8 分版，数字全换）

考场上数字会变，但流程一模一样。模拟卷这道：输入层 $x_1=0.5, x_2=0.1$，阈值节点 $x_0=-1$；隐层 2 节点，阈值节点 $y_0=-1$；输出层 1 节点，期望输出 $d=1$，激活函数还是单极性 Sigmoid。

权值换成了：$V=\begin{bmatrix} 0.2&0.1&-0.1\\ 0.3&-0.2&0.4 \end{bmatrix}$，$W=\begin{bmatrix} 0.2&0.5&-0.3 \end{bmatrix}$。

**（1）隐层净输入和输出**（3 分）：

$$\text{net}_{y1} = 0.2\times(-1) + 0.1\times0.5 + (-0.1)\times0.1 = -0.2+0.05-0.01 = -0.16$$

$$y_1 = f(-0.16) = \frac{1}{1+e^{0.16}} \approx 0.4602$$

$$\text{net}_{y2} = 0.3\times(-1) + (-0.2)\times0.5 + 0.4\times0.1 = -0.3-0.1+0.04 = -0.36$$

$$y_2 = f(-0.36) = \frac{1}{1+e^{0.36}} \approx 0.4109$$

**（2）输出层实际输出和误差**（3 分）：

$$\text{net}_o = 0.2\times(-1) + 0.5\times0.4602 + (-0.3)\times0.4109 = -0.2+0.2301-0.1233 = -0.0932$$

$$o = f(-0.0932) = \frac{1}{1+e^{0.0932}} \approx 0.4767$$

$$E = \frac{1}{2}(1-0.4767)^2 \approx \frac{1}{2}\times0.2738 \approx 0.1369$$

**（3）输出层误差信号**（2 分）：

$$\delta^o = (d-o)\cdot o(1-o) = 0.5233 \times 0.4767 \times 0.5233 \approx 0.5233\times0.2493 \approx 0.1305$$

看出来了吧？连**期望输出 $d=1$ 和 $d=0.9$** 这种细节都在变，但三步走的骨架雷打不动：前向 → 算误差 → 反传误差信号。把步骤写全，分数就到手。

### BP 考点速记（填空/选择版）

- 填空：激活函数必须处处**可微**，最常用 **S（Sigmoid）** 型；
- 填空：输出层误差信号 $\delta_k^o = (d_k-o_k)\cdot o_k(1-o_k)$（即对数 S 型函数的导数形式），隐层误差信号通过**误差反向传播**获得；
- 填空：两大固有缺点是存在**平坦**区域和多个**局部极小**点；
- 选择题：附加动量法的作用是「利用动量项使权值调整趋向误差曲面底部平均方向，有助于跳出局部极小」；
- 综合题：二维数据分 4 类，BP 输入层 2 节点、输出层 4 节点（用「n 中取 1」编码：1000、0100、0010、0001），隐层先试 1 层，按 $m=\sqrt{n+l}+\alpha$ 取 3~7 个节点开始试。

## 四、卷积神经网络（CNN）

### 三大特性：CNN 的灵魂

1. **局部连接**：每个神经元只跟输入的一个局部区域（**感受野**）相连，不跟全图眉来眼去；
2. **权重共享**：同一个卷积核在图像各处滑动时**共用同一组权值**，翻译过来就是「同一把尺子量全图」；
3. **空间/时间次采样（池化）**：隔三差五把图缩小一号。

三大特性合力带来两大核心优势（简答必背）：**显著减少参数数量** + **有效提取局部不变性特征**（平移、缩放、旋转都不怕）。填空题「三大结构特性是局部连接、**权重共享**和**空间或时间上的次采样（池化）**」——送分题。简答第 3 题里「局部连接」和「权重共享」的含义各值 2 分：局部连接 = 每个神经元只与输入的一个局部区域（感受野）连接，而不是连全部输入节点；权重共享 = 同一个卷积核在输入不同位置滑动时共享同一组权值参数。

### 卷积操作细节

卷积核就是一个小滤波器，配合**步长 $S$** 和**零填充 $P$** 在图上滑动。尺寸公式（必考！）：

$$O = \left\lfloor \frac{I - K + 2P}{S} \right\rfloor + 1$$

- **窄卷积**：$P=0$，输出长度 $M-K+1$，图会越卷越小；
- **等宽卷积**：$P=(K-1)/2$，输出长度不变 $=M$，**目前默认做法**。

冷知识：实际应用中说的「卷积」通常指**互相关**——就是**不翻转核**直接算，别纠结数学定义，考试按互相关来。

二维卷积维度规则：输入图像 $H\times W\times C$，用 $k\times k\times C$ 的卷积核（通道数要对上），**输出特征图的通道数 = 卷积核的个数**。比如 96 个核就是 96 个通道。

### 池化（汇聚层）

池化的作用是**降低特征图尺寸、减少计算量、提供平移不变性**。最常用最大池化，$2\times2$ 窗口步长 2，尺寸直接对半砍。它就像给特征图「降采样」，保大弃小。

### 经典网络演进四巨头（时间线要背）

- **LeNet-5**：7 层网络，手写数字识别的鼻祖，CNN 的祖师爷；
- **AlexNet**：2012 年 ILSVRC 冠军，一战封神。三大杀器：**ReLU** 激活（解决梯度饱和）、**Dropout**（随机丢弃神经元防过拟合）、**数据增强**，还用了 GPU 并行；
- **GoogLeNet**：核心是 **Inception 模块**，把 $1\times1$、$3\times3$、$5\times5$ 多种尺度卷积**拼接**起来，多尺度特征一网打尽，整个网络参数仅 **4M**，又准又苗条；
- **ResNet**：**残差单元（跳跃连接）**让梯度能「抄近道」直接传到前面，**解决梯度消失**，深度做到 **152 层+**，2015 年冠军。选择题问谁用残差单元——闭眼选 ResNet。

### 空洞卷积与转置卷积

- **空洞卷积**：在核里「打洞」，不增加参数就能**扩大感受野**；
- **转置卷积（反卷积）**：卷积的逆操作，用来**上采样**放大特征图。

应用场景：图像分类、目标检测、OCR、图像增强、风格迁移——从认猫到 P 图全包了。

### CNN 计算题套路（7 分题实战）

输入 $32\times32\times3$ 的 RGB 图：(1) 第一层 64 个 $5\times5$ 核，$S=1, P=2$——等宽卷积，输出 $32\times32$，通道数=64，即 **$32\times32\times64$**；(2) 接 $2\times2$ 最大池化 $S=2$——尺寸对半，**$16\times16\times64$**；(3) 第二层 128 个 $3\times3$ 核，$S=1, P=1$——还是等宽，**$16\times16\times128$**。

还有经典的 $227\times227$ 题：96 个 $11\times11$ 核，$S=4, P=0$，代入公式 $(227-11)/4+1 = 55$，答案是 **$55\times55$**（选 A）。注意这里是窄卷积，别手滑加填充。

### LeNet 风格网络：逐层尺寸 + 参数量（10 分题实战）

专项训练第 2 题把尺寸和参数串在一起考，输入 $32\times32\times1$ 灰度图，结构 C1（6 个 $5\times5$ 核，$S=1, P=0$）→ P1（$2\times2$ 池化，$S=2$）→ C2（16 个 $5\times5$ 核，$S=1, P=0$）→ P2（$2\times2$ 池化，$S=2$）→ FC（120 输出节点）。

**逐层尺寸**（4 分）：

- C1：$(32-5)/1+1 = 28$，即 **$28\times28$**，通道 6；
- P1：$28/2 = 14$，**$14\times14$**，通道 6；
- C2：$(14-5)/1+1 = 10$，**$10\times10$**，通道 16；
- P2：$10/2 = 5$，**$5\times5$**，通道 16；
- FC 的输入就是 $5\times5\times16 = 400$ 维。

**参数量（含偏置）**（4 分）：

- C1 层 6 个 $5\times5\times1$ 核含偏置 = $(5\times5\times1+1)\times6 = 156$；
- C2 层 16 个 $5\times5\times6$ 核 = $(5\times5\times6+1)\times16 = 2416$；
- 全连接层 $(400+1)\times120 = 48120$。

**输入换成 RGB**（2 分）：输入通道变 3，C1 立刻变成 $(5\times5\times3+1)\times6 = 456$——记住：**核的深度跟着输入通道走**。这一串数字考场上要能从头推到尾，不许翻笔记。

## 五、自组织竞争网络：没有老师也能学

### WTA 竞争学习

**胜者为王（Winner-Take-All）**三步走：先把向量**归一化**（模长变 1）→ 找**获胜神经元**（和输入点积最大 / 距离最小）→ **只调整获胜者**的权值。输家原地不动，只有冠军吃肉。

### SOFM（Kohonen 自组织特征映射）

SOFM 是竞争学习的豪华升级版：

- **拓扑结构**：竞争层排成一维线阵或二维平面阵；
- **优胜邻域**：不只是冠军调整，**以获胜神经元为中心的一个邻域**里的神经元都按「离冠军越近调得越多」的方式调整；**邻域随训练不断收缩，最后缩到 0**——先是全班一起学，最后只剩尖子生自己学；
- **学习率退火**：$\eta(t)$ 先大后小，像温度一样慢慢降下来；
- **权值初始化**：尽量让初始权向量**与输入样本分布重合**——用全体样本的中心向量叠加上小随机数，或者直接从训练集里随机抽样本当初始权值，避免所有神经元最后挤成一团。

注意选择题陷阱：SOFM 训练时**并不是只有获胜神经元调权值**，邻域内的也要调（选 C 的说法是错的）；邻域半径随训练**逐渐收缩**（这是对的）。

### LVQ（学习向量量化）

LVQ 是**有导师的竞争网络**，带上了类别标签：分类**正确**，权值就**向**样本方向靠近（奖励）；分类**错误**，权值就**远离**样本（惩罚）。分类正确时它的更新公式是 $W^* = W^* + \alpha(X - W^*)$，跟 SOFM 获胜神经元的更新**方向完全相同**——所以两者方向一致，差别只在**调不调邻域**：LVQ 通常只调获胜神经元。选择题考过：LVQ 输出层到竞争层的权值在训练中**固定为 1 不变**（选 B）。

SOFM vs LVQ 对比（简答第 4 题，三个维度）：

- **学习方式**：SOFM 无导师自组织；LVQ 有导师监督，用类别标签指导竞争层；
- **调整范围**：SOFM 调获胜者 + 优胜邻域；LVQ 通常只调获胜神经元；
- **调整方向**：SOFM 权值永远向输入样本方向调；LVQ 看分类对不对，对则靠近、错则远离。

### CPN 与 ART

- **CPN（对偶传播网络）**：**两阶段学习**——隐层用竞争学习（**内星规则**），输出层用有导师学习（**外星规则**）；
- **ART（自适应共振理论）**：专门解决**稳定性-可塑性困境**（学了新知识别把旧的忘光，但又要有能力学新的）。四阶段：**匹配 → 比较 → 搜索 → 学习**。关键是**警戒门限 $\rho$**：$\rho$ 越大，模式分得越细，**类别数越多**（填空答案：警戒；多）。

### SOFM 计算题示例

输入 $X=[0.8, 0.6]^T$（已归一化），三个神经元的权向量 $W_1=[1,0]^T$、$W_2=[0,1]^T$、$W_3=[0.7071,0.7071]^T$。点积相似度：$X\cdot W_1=0.8$、$X\cdot W_2=0.6$、$X\cdot W_3 \approx 0.9899$，**神经元 3 获胜**。

用学习率 $\alpha=0.5$ 更新：$W_3^{new} = W_3 + 0.5(X-W_3) = [0.7536, 0.6536]^T$，再归一化（模长 $\approx 0.9974$）得到 $[0.7555, 0.6553]^T$。注意 LVQ 若分类正确，方向跟 SOFM 一样是靠近样本，但**不调邻域**。

综合题里 SOFM 的设计原则：竞争层神经元数**不少于类别数**，可以多设（比如 16~25 个），用**二维平面阵**（$4\times4$ 或 $5\times5$）直观反映样本拓扑；权值初始化要避免全聚成一类。

## 六、RBF 网络：局部英雄

### 概念与结构

**RBF（径向基函数）网络**是**局部逼近网络**——对某一局部区域，只有少数神经元决定输出（选择题选 C 的那个「独有特点」）。隐层用径向对称的高斯核：

$$\varphi(\|X-c_i\|) = \exp\left(-\frac{\|X-c_i\|^2}{2\sigma_i^2}\right)$$

离中心 $c_i$ 越远，输出越小，像一盏只照亮周围的小灯。结构：**输入 → 隐层（RBF 非线性映射）→ 输出层（线性）**。填空答案：RBF 是**局部**逼近网络，隐层常用**高斯（径向基）**函数。

### 参数学习

- 中心 $c_i$：用**模糊 K 均值聚类**确定；
- 宽度 $\sigma_i$：根据聚类结果确定；
- 输出权值 $\omega$：梯度下降 / LMS / 最小二乘。

### MATLAB 两兄弟

- **newrbe**：精确设计，**隐节点数 = 样本数**，训练误差为 0。听着完美，但样本多了网络就庞大，实时性差、容易过拟合——100 个样本就是 100 个隐节点，10000 个就是 10000 个；
- **newrb**：迭代设计，**从 0 开始逐个增加隐层神经元**，直到误差降到目标值以下。网络规模可控、计算量小，更适合实际应用。

### RBF 设计分析题（模拟卷原题）

某函数逼近问题有 **100 个训练样本**，三个小问把 MATLAB 两兄弟和 GRNN 一网打尽：

**（1）用 `newrbe` 精确设计**（3 分）：隐层神经元个数**等于输入样本个数，即 100 个**。优点：对训练样本达到**零误差**精确拟合；缺点：样本数大时网络过于庞大，实时性差，泛化能力可能不足（易过拟合）。

**（2）改用 `newrb` 设计**（2 分）：采用**迭代方法**，从 0 开始逐个增加隐层神经元，直到平方和误差下降到目标值以下或达到最大神经元数为止。优势：**网络规模可控、计算量小**，更适合实际应用。

**（3）GRNN 与正规化 RBF 的区别**（2 分）：正规化 RBF 隐节点数 $M$ **等于**样本数 $P$（$M=P$）；GRNN（广义 RBF）的 $M$ **小于** $P$（通常 $N<M<P$，$N$ 为输入维数），且中心**不限于数据点**，由训练算法确定。

### GRNN 与 PNN

- **GRNN（广义回归网络）**：隐节点数 $M$ **小于**样本数 $P$（通常 $N<M<P$，$N$ 为输入维数），中心**不限于数据点**；
- **PNN（概率神经网络）**：隐节点数 = 样本数，**输出节点数 = 类别数**，输出层竞争，理论上**收敛于贝叶斯分类器**。

### RBF 高斯核计算题

输入 $X=[0.5,0.3]^T$，两个隐节点：中心 $[0,0]^T$、$\sigma=0.5$、权值 0.4；中心 $[1,1]^T$、$\sigma=0.8$、权值 -0.6；输出偏置 $\theta=0.1$。

先算欧氏距离：$d_1=\sqrt{0.5^2+0.3^2}\approx0.5831$，$d_2=\sqrt{0.5^2+0.7^2}\approx0.8602$。

再算高斯输出：$\varphi_1=\exp(-0.34/(2\times0.5^2))=\exp(-0.68)\approx0.5066$，$\varphi_2=\exp(-0.74/(2\times0.8^2))\approx0.5610$。

最后线性组合：$y = 0.4\times0.5066 + (-0.6)\times0.5610 + 0.1 \approx -0.0339$。三步走：**距离 → 高斯 → 加权求和**，考场上按计算器就能拿下。

## 七、遗传算法（GA）与网络进化

### 三大操作

1. **选择**：**轮盘赌**（适应度比例复制）——适应度越高，在轮盘上占的扇区越大，越容易被选中，但运气差的小个体偶尔也能被抽中；
2. **交叉**：单点/多点交换染色体片段，概率 $P_c$ 取 **0.6~0.9**；
3. **变异**：小概率翻转基因位，概率 $P_m$ 取 **0.001~0.1**。填空答案：三个基本操作是选择、**交叉**和**变异**。

### 模式定理

模式 $H$ 的两个指标：**模式次数 $O(H)$**（确定位的个数）和**定义长度 $\delta(H)$**（首尾确定位之间的距离）。结论（选择题选 B）：**短、低阶、高适应度的模式在后代中呈指数级增长的可能性较大**——这就是「积木块假设」，进化论在算法里的化身。

### 编码与适应度

- **编码**：二进制编码最常见，也可以实数编码；
- **适应度函数**：通常 $F = C/E$ 或 $F = 1/E$——**误差越小，适应度越大**，越容易活到下一代（综合题答案里还会写成 $F=1/E$ 或 $F=C-E$，一个意思）。

### 参数与改进策略

参数三件套：种群规模 $n$、交叉概率 $P_c$（0.6~0.9）、变异概率 $P_m$（0.001~0.1）。改进策略：自适应变异、部分替换、**精英保留**（最优秀个体直接保送下一代，不会被变异毁掉）、移民法、分布式 GA。

### GA 与 NN 结合：让网络自己进化

- **权值进化**：把所有权值编码成一条染色体，以网络误差为适应度；
- **结构进化**：直接编码（连接矩阵）或间接编码（层数、节点数）；
- **权值与结构共同进化**。

简答第 5 题「GA 优化 BP 权值的基本步骤」五步走：**编码**（所有权值和阈值编成二进制/实数染色体）→ **初始化**（随机生成初始种群）→ **适应度计算**（解码成权值，算训练误差 $E$，取 $F=1/E$）→ **遗传操作**（轮盘赌选择 + 交叉 + 变异，产生新一代）→ **终止判断**（达到最大进化代数或误差达标，解码最优个体当最终权值）。评分是 1+1+1+2+1，遗传操作那步最贵，记得展开写。

综合题里算过：网络结构 2-5-4，权值+阈值总数 = $2\times5+5\times4+5+4 = 39$，就是一条 39 维染色体。

### 轮盘赌计算题

4 个个体适应度 2.5、4.0、1.5、6.0：总适应度 $=14.0$，选择概率分别是 0.1786、0.2857、0.1071、0.4286，累积概率 0.1786 → 0.4643 → 0.5714 → 1.0000。随机数 0.35 落在 $(0.1786, 0.4643]$ 选 **2 号**；0.72 落在 $(0.5714, 1]$ 选 **4 号**；0.15 落在 $[0, 0.1786]$ 选 **1 号**；0.90 还是选 **4 号**。套路：**随机数找它落在哪个累积概率区间，就选那个个体**。加精英保留的话，最优的 4 号直接保送，剩下 3 个名额按轮盘赌结果（2、4、1）来，新一代种群就是 **{4, 2, 4, 1}**——注意里面有两个 4 号，一个保送的一个抽中的，允许重复。

## 八、期末模拟试题精华速览

### 填空题 10 连（原题 + 答案速记）

1. 人工神经网络是对人脑的一阶特性描述，具有高度的______性、良好的容错性和______能力。→ **非线性**（或非线）；**自学习**（或自组织/自适应，任选其一）；
2. M-P 模型中，神经元的工作状态分为______、兴奋和抑制三种；当膜电位超过______mV 时，细胞产生兴奋脉冲。→ **静止**；**-55**（或阈值/约 -55）；
3. BP 网络的激活函数必须是处处______的，因此不能采用二值型阈值函数，最常使用的是______型激活函数。→ **可微**；**S**（Sigmoid/对数 S 型/正切 S 型）；
4. 在标准 BP 算法中，输出层误差信号 $\delta_k^o = (d_k-o_k) \cdot$ ______；隐层误差信号通过______传播获得。→ **$o_k(1-o_k)$**（对数 S 型函数的导数形式）；**误差反向**（反向传播/逐层反传）；
5. BP 算法的两大固有缺点是存在______区域和多个______点。→ **平坦**；**极小**（局部极小）；
6. 卷积神经网络的三大结构特性是局部连接、______和______。→ **权重共享**；**空间或时间上的次采样**（池化/subsampling，答对核心词即可）；
7. 在 SOFM 网络中，权值调整不仅针对获胜神经元，还包括其______内的神经元，且邻域大小随训练进行不断______。→ **优胜邻域**；**收缩**（减小/变小）；
8. RBF 网络是一种______逼近网络，其隐层常用______函数作为激活函数。→ **局部**；**高斯**（径向基/RBF）；
9. 遗传算法的三个基本操作是选择、______和______。→ **交叉**；**变异**；
10. 在 ART 网络中，通过调整______门限 $\rho$ 的大小可以控制模式的类别数；$\rho$ 越大，类别数越______。→ **警戒**；**多**（大/高）。

### 选择题 10 连（原题 + 选项 + 答案）

1. 人脑与计算机比较，说法**错误**的是（　）。A. 人脑并行、计算机串行；B. 人脑分布式存储、计算机集中式；C. 人脑擅长精确数值计算，计算机擅长模糊识别；D. 人脑有综合归纳能力。→ **C**（正好说反）；
2. 不满足 BP 网络对激活函数要求的是（　）。A. Sigmoid；B. tanh；C. 线性函数；D. 二值型阈值函数 {0,1}。→ **D**（不可微）；
3. 附加动量法的作用，描述最准确的是（　）。A. 加快平坦区学习；B. 利用动量项使权值调整趋向误差曲面底部平均方向，有助于跳出局部极小；C. 自动调整学习率防止过调；D. 减少隐层神经元。→ **B**；
4. 输入 $227\times227\times3$，96 个 $11\times11$ 核，$S=4, P=0$，输出尺寸（　）。A. $55\times55$；B. $56\times56$；C. $54\times54$；D. $27\times27$。→ **A**；
5. 关于 SOFM，说法**错误**的是（　）。A. 无导师学习；B. 竞争层按一维或二维拓扑排列；C. 训练时只有获胜神经元调整权值；D. 优胜邻域半径随训练逐渐收缩。→ **C**（邻域内的也调）；
6. RBF 相比 BP **独有**的特点是（　）。A. 非线性映射能力；B. 梯度下降训练权值；C. 局部逼近网络，对某一局部区域只有少数神经元决定输出；D. 存在局部极小值问题。→ **C**；
7. 模式 $H$ 定义长度小、模式次数低，经过选择交叉变异后（　）。A. 一定消失；B. 在后代中呈指数级增长的可能性较大；C. 只受交叉影响；D. 适应度高低对存活没影响。→ **B**；
8. 关于 LVQ，说法**正确**的是（　）。A. 完全无监督；B. 输出层到竞争层的权值在训练中固定为 1 且不变；C. 竞争层神经元数必须等于输入样本数；D. 权值调整规则与 SOFM 完全相同。→ **B**；
9. 设计 BP 时隐层节点数最合理的做法（　）。A. 越多越好；B. 越少越好；C. 先设较少节点训练，逐渐增加，按网络误差最小确定；D. 必须严格按公式 $m=\sqrt{n+l}$ 计算。→ **C**；
10. 采用残差单元解决梯度消失的是（　）。A. LeNet-5；B. AlexNet；C. GoogLeNet；D. ResNet。→ **D**。

### 简答题 5 道（原题 + 答题骨架 + 评分）

1. **简述人工神经网络的基本特点与主要功能。**（6 分）特点四选三（分布存储容错/并行处理/自学习自组织自适应/高度非线性），功能五选三（联想记忆/非线性映射/分类识别/优化计算/知识处理），每点 1 分，答出 3 点即满分；
2. **什么是 BP 算法的「平坦区域」问题？分析其产生原因及克服方法。**（6 分）定义（梯度小、误差降得慢，1 分）→ 原因（S 型饱和区、$\delta$ 接近 0，2 分）→ 克服（附加动量/自适应学习率/陡度因子/改进误差函数，答 3 点每点 1 分）；
3. **简述 CNN 相比全连接前馈网络的两个核心优势，并说明「局部连接」和「权重共享」的含义。**（6 分）两大优势 2 分（减少参数 + 提取局部不变特征）；局部连接 = 只连感受野 2 分；权重共享 = 同一核滑动共用权值 2 分；
4. **比较 SOFM 与 LVQ 在学习方式、权值调整策略上的主要区别。**（6 分）无导师 vs 有导师 2 分；调邻域 vs 只调获胜者 2 分；总靠近 vs 对则近错则远 2 分；
5. **简述 GA 在神经网络权值优化中的基本步骤。**（6 分）编码 → 初始化 → 适应度（$F=1/E$）→ 遗传操作（2 分）→ 终止判断，其余各 1 分。

### 计算题 4 大类型（考前必练）

1. **BP 前向 + 反向**：隐层净输入 → Sigmoid → 输出层 → 误差 → 误差信号逐层回传 → $\Delta w = \eta\delta\times$输入；
2. **CNN 尺寸与参数**：$O=\lfloor(I-K+2P)/S\rfloor+1$，通道数 = 核个数，参数 = (核体积×输入通道 + 1)×核个数；
3. **RBF 高斯核**：欧氏距离 → $\exp(-d^2/2\sigma^2)$ → 加权求和；
4. **SOFM 与轮盘赌**：点积找冠军 + 更新再归一化；累积概率区间匹配。

### 综合设计题（8 分，400 个样本分 4 类）

某模式识别任务要把二维数据分成 4 类，样本共 400 个，三问横跨 BP、SOFM、GA 三章：

**（1）BP 方案**（3 分）：输入层 **2 个节点**（二维数据）；输出层 **4 个节点**（4 分类），编码用 **「n 中取 1」**（1000、0100、0010、0001）或 **「n-1」编码**；隐层先尝试 1 层，节点数用试凑法或经验公式 $m=\sqrt{n+l}+\alpha$，此处 $n=2, l=4$，$m$ 约取 **3~7** 个开始尝试。

**（2）SOFM 方案**（3 分）：竞争层神经元数**不少于类别数**，先设较多节点（如 **16~25 个**），用**二维平面阵**（$4\times4$ 或 $5\times5$）直观反映样本拓扑；权值初始化要避免全部聚为一类——先算全体样本中心向量再叠加小随机数，或直接从训练集随机抽样本当初始权值，让初始权向量与输入样本分布充分重合。

**（3）GA 优化 BP 权值**（2 分）：把 BP 网络所有连接权值和阈值按层连接顺序编码为一个长串（二进制或实数），例如网络结构 2-5-4 时权值+阈值总数 $=2\times5+5\times4+5+4=39$，编码成 39 维向量；适应度以网络误差函数 $E$（如 MSE）的倒数表示，即 $F=1/E$ 或 $F=C-E$，误差越小适应度越高，越容易被选择遗传到下一代。

这道题把三章的考点焊在一张卷子里，考前一定亲手写一遍。

## 写在最后：备考建议

计算题占卷面 **30%~40%**，是拉分大头，务必带好计算器并掌握按键顺序（`e^x` / `EXP`）。重点攻克五件事：**Sigmoid 及其导数的求值**、**BP 链式求导的逐层回传逻辑**、**CNN 尺寸公式**、**RBF 高斯核的指数运算**、**轮盘赌的累积概率区间匹配**。

知识点背不进去的时候，就把它们当成小剧场：Sigmoid 是爱偷懒的打工人（输出 0 或 1 附近就躺平），Dropout 的哲学是**别把希望都押在某一个神经元上**，ResNet 的跳跃连接是给梯度开的「逃生通道」，轮盘赌则是大自然的抽奖机。理解比死记重要，考场上公式写对、步骤写全，分就到手啦。祝我们都顺利上岸！
